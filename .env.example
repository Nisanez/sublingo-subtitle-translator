# Optional environment configuration for SubLingo.
# Copy to ".env" only if you need to override defaults (the app also has an
# in-app "Ollama host" field that takes precedence).

# Base URL of your Ollama server.
# Default: http://localhost:11434
OLLAMA_URL=http://localhost:11434
